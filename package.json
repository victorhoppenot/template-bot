{
  "name": "template",
  "description": "it's a template",
  "version": "0.0.0",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "npm i mysql && npm i fs && npm i enmap"
    "_comment": "//mysql for database; fs for file system; enmap for enmaps" 
  },
  "dependencies": {
    "discord.js": "11.1.0",
    "request": "2.81.0"
  }
}
